<%= form_for([@post, @post.comments.build]) do |f| %>
<p>
	<%= f.label "Dari" %><br />
	<%= f.text_field :commenter %>
</p>
<p>
	<%= f.label "Komentar" %><br />
	<%= f.text_area :body %>
</p>
<p>
	<%= f.submit "Tambah Komentar", :class => "btn btn-success" %>
</p>
<% end %>